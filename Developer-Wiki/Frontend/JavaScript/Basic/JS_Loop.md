---
title: "JavaScript 반복문"
area: "JavaScript"
version: "v4.3"
last_updated: "2026-07-27"
difficulty: "★★☆☆☆"
estimated_time: "35~55분"
---

# JavaScript 반복문

## 학습 목표

- for와 while을 사용해 같은 작업을 반복한다.
- 예제의 실행 순서를 설명한다.
- 현재까지 배운 문법으로 문제를 해결한다.

## 왜 배우는가

JavaScript는 값과 화면을 연결하고 사용자 행동에 반응하게 만듭니다. 새로운 문법을 짧게 쓰는 것보다 실행 흐름을 정확히 이해하는 것이 우선입니다.

## 기본 개념

```javascript
for (let number = 1; number <= 5; number++) {
    console.log(number);
}
```

## 수업 예제

예제를 직접 입력한 뒤 변수값과 실행 순서를 `console.log()`로 확인합니다.

## 수업 문제

### 문제

1부터 10까지의 짝수만 출력하고 짝수의 합계도 출력하세요.

### 요구사항

- for문을 사용합니다.
- 나머지 연산자로 짝수를 확인합니다.
- 합계를 저장할 변수를 반복문 밖에 선언합니다.

### 직접 풀어 보기

해설을 열기 전에 입력값, 처리 과정, 출력 결과를 나누어 적고 코드를 작성합니다.

<details>
<summary>해설 보기</summary>

```javascript
let total = 0;

for (let number = 1; number <= 10; number++) {
    if (number % 2 === 0) {
        console.log(number);
        total += number;
    }
}

console.log('합계:', total);
```

### 풀이 설명

현재 문서까지 배운 문법을 중심으로 작성했습니다. 먼저 기본 풀이를 이해한 뒤 더 알아보기의 짧은 문법과 비교합니다.

</details>

## 자주 하는 실수

- 종료 조건이 없어 무한 반복되는 경우
- 증감식 위치를 잘못 작성하는 경우
- 반복할 때마다 합계 변수를 다시 0으로 만드는 경우

## 실무 연결

목록 출력, 합계 계산, 여러 DOM 요소 처리처럼 같은 규칙을 여러 값에 적용할 때 사용합니다.

## 📌 더 알아보기

`break`는 반복을 즉시 끝내고 `continue`는 현재 반복만 건너뜁니다. 기본 반복 흐름을 익힌 뒤 사용합니다.

## 직접 해보기

- 예제의 값과 조건을 변경하고 결과를 예상한다.
- 중간 변수값을 출력해 실행 흐름을 확인한다.
- 오류가 발생한 줄과 오류 메시지를 읽고 원인을 기록한다.

## Check Point

- [ ] for문의 초기식·조건식·증감식을 설명할 수 있다.
- [ ] 반복 횟수를 예상할 수 있다.
- [ ] 누적 합계를 계산할 수 있다.

## 최종 요약

for와 while을 사용해 같은 작업을 반복한다.

## 복습 기록

- [ ] 예제를 직접 입력했다.
- [ ] 기본 풀이를 해설 없이 작성했다.
- [ ] 더 알아보기 문법과 기본 풀이를 비교했다.

## 이전 · 다음 학습

| 구분 | 문서 |
|---|---|
| 이전 학습 | [JavaScript 연산자와 조건문](JS_Operator_Condition.md) |
| 다음 학습 | [JavaScript 함수와 화살표 함수](JS_Function_Arrow.md) |
