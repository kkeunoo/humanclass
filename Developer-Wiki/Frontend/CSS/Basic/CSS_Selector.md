---
title: "CSS 선택자와 우선순위"
area: "CSS"
version: "v4.3"
last_updated: "2026-07-27"
difficulty: "★★☆☆☆"
estimated_time: "30~50분"
---

# CSS 선택자와 우선순위

## 학습 목표

- 요소 선택자, 클래스 선택자, 아이디 선택자를 구분하고 지정된 요소에만 스타일을 적용한다.
- 개발자 도구에서 최종 적용된 스타일을 확인한다.
- 해당 속성을 사용하는 이유를 설명한다.

## 왜 배우는가

CSS는 HTML 구조를 읽기 쉽고 사용하기 좋은 화면으로 표현합니다. 이 문서의 속성은 실제 UI의 크기, 간격, 배치와 상태 표현에 직접 사용됩니다.

## 기본 개념

```css
p { color: #333; }
.notice { font-weight: bold; }
#main-title { font-size: 32px; }
.container p { line-height: 1.7; }
```

선택자는 스타일을 적용할 대상을 지정합니다. 클래스는 반복 가능한 그룹, 아이디는 한 문서의 고유 요소에 사용합니다.

## 수업 예제

예제를 HTML 요소에 적용한 뒤 개발자 도구의 **Styles**와 **Computed**에서 최종 값을 확인합니다.

## 수업 문제

### 문제

제목, 공지 문단, 일반 문단에 서로 다른 선택자를 사용해 스타일을 적용하세요.

### 요구사항

- 제목은 아이디 선택자를 사용합니다.
- 공지 문단은 클래스 선택자를 사용합니다.
- 컨테이너 안의 일반 문단에만 자손 선택자를 사용합니다.

### 직접 풀어 보기

해설을 열기 전에 선택자와 속성을 직접 작성하고 브라우저 너비 또는 요소 값을 바꾸어 확인합니다.

<details>
<summary>해설 보기</summary>

```css
#main-title { font-size: 32px; }
.notice { font-weight: bold; }
.container p { line-height: 1.7; }
```

### 풀이 설명

요구사항에 필요한 속성만 사용했습니다. 각 선언을 한 줄씩 제거해 보며 어떤 역할을 하는지 확인합니다.

</details>

## 자주 하는 실수

- 선택자 철자를 HTML과 다르게 작성하는 경우
- 아이디 선택자를 반복 요소에 사용하는 경우
- 너무 긴 선택자로 우선순위를 불필요하게 높이는 경우

## 실무 연결

컴포넌트의 상태와 역할에 따라 클래스를 설계하면 스타일 충돌을 줄일 수 있습니다.

## 📌 더 알아보기

우선순위가 충돌하면 더 구체적인 선택자나 뒤에 선언된 규칙이 적용될 수 있습니다. `!important`는 기본 해결책으로 사용하지 않습니다.

## 직접 해보기

- 속성값을 하나씩 변경하고 화면 차이를 비교한다.
- 개발자 도구에서 덮어쓴 선언과 최종 계산값을 확인한다.
- 같은 결과를 만들 수 있는 다른 속성이 있는지 기록한다.

## Check Point

- [ ] 태그·클래스·아이디 선택자를 구분할 수 있다.
- [ ] 자식과 자손 선택자의 차이를 설명할 수 있다.
- [ ] 개발자 도구에서 적용된 규칙을 확인할 수 있다.

## 최종 요약

요소 선택자, 클래스 선택자, 아이디 선택자를 구분하고 지정된 요소에만 스타일을 적용한다.

## 복습 기록

- [ ] 예제를 직접 작성했다.
- [ ] 문제를 해설 없이 풀었다.
- [ ] 개발자 도구에서 적용 결과를 확인했다.

## 이전 · 다음 학습

| 구분 | 문서 |
|---|---|
| 이전 학습 | [CSS README](../README.md) |
| 다음 학습 | [CSS 글꼴과 텍스트](CSS_Font.md) |
