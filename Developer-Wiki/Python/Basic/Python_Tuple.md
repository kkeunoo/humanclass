---
title: "Python Tuple"
area: "Python"
version: "v4.3"
last_updated: "2026-07-27"
difficulty: "★★☆☆☆"
estimated_time: "25~40분"
---

# Python Tuple

## 학습 목표

- 튜플의 생성과 인덱싱을 이해하고 리스트와 차이를 비교한다.
- 터미널 또는 Python 실행 환경에서 직접 결과를 확인한다.
- 오류 메시지의 줄 번호와 원인을 확인한다.

## 왜 배우는가

Python은 데이터를 저장하고 처리하는 문법을 간결하게 표현합니다. 직접 실행하고 출력 결과를 확인하는 과정이 개념 이해의 핵심입니다.

## 기본 개념

```python
point = (10, 20)

print(point[0])
print(point[1])
```

## 수업 예제

예제를 실행한 뒤 변수의 값이나 코드 일부를 한 번에 하나씩 바꾸고 결과 차이를 확인합니다.

## 수업 문제

### 문제

좌표 튜플을 만들고 x와 y 값을 각각 출력하세요.

### 요구사항

- 튜플 값은 10과 20입니다.
- 먼저 인덱스로 두 값을 출력합니다.
- 튜플 요소를 직접 변경하지 않습니다.

### 직접 풀어 보기

해설을 열기 전에 실행 결과를 먼저 예상하고 코드를 작성합니다.

<details>
<summary>해설 보기</summary>

```python
point = (10, 20)

print(point[0])
print(point[1])
```

### 풀이 설명

현재 문서까지 배운 문법만 사용했습니다. 코드를 위에서 아래로 읽으며 각 변수와 출력문의 역할을 확인합니다.

</details>

## 자주 하는 실수

- 튜플 요소를 리스트처럼 변경하려는 경우
- 한 개짜리 튜플에서 쉼표를 빠뜨리는 경우
- 괄호만 있으면 모두 튜플이라고 생각하는 경우

## 실무 연결

변경하지 않을 좌표, 색상값, 고정된 설정값 묶음을 표현할 때 사용할 수 있습니다.

## 📌 더 알아보기

### 튜플 언패킹

```python
point = (10, 20)
x, y = point
```

튜플의 각 값을 여러 변수에 나누어 저장할 수 있습니다. 함수의 여러 반환값과 연결되는 사용법은 함수 학습 이후 다룹니다.

## 직접 해보기

- 변수값이나 문자열을 변경하고 결과를 예상한다.
- `print()`와 `type()`으로 값과 자료형을 확인한다.
- 오류를 한 번 직접 만들고 메시지와 줄 번호를 읽는다.

## Check Point

- [ ] 리스트와 튜플의 차이를 설명할 수 있다.
- [ ] 튜플 값에 인덱스로 접근할 수 있다.
- [ ] 한 개짜리 튜플에 쉼표가 필요한 이유를 설명할 수 있다.

## 최종 요약

튜플의 생성과 인덱싱을 이해하고 리스트와 차이를 비교한다.

## 복습 기록

- [ ] 예제를 직접 실행했다.
- [ ] 문제를 해설 없이 풀었다.
- [ ] 오류 메시지를 읽고 수정했다.

## 이전 · 다음 학습

| 구분 | 문서 |
|---|---|
| 이전 학습 | [Python List와 range](Python_List_Range.md) |
| 다음 학습 | [Developer-Wiki 홈](../../README.md) |
